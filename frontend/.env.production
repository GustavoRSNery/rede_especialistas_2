# =========================================================
# Frontend — Variáveis de ambiente para o Vite (build time)
# Prefixo VITE_ obrigatório para expor ao bundle do browser.
# =========================================================

# URL base da API do backend acessada pelo browser via Nginx
VITE_BACKEND_API_URL=/backend/api
